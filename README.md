# 🍲 My Recipe Box - Full Stack Application

A robust, full-stack recipe sharing platform built as a capstone internship project. It allows users to securely register, publish complex structured recipes, interact via comments and ratings, and discover new dishes using a custom search engine.

## 🚀 Live Links (Graders Start Here!)
* **Live Website:** [Paste your Vercel URL here, e.g., https://recipebox-frontend.vercel.app]
* **Live API Backend:** [Paste your Render URL here, e.g., https://recipebox-api...onrender.com]

## 🛠️ Tech Stack
* **Frontend:** React.js, Vercel, Context API, CSS3
* **Backend:** Node.js, Express.js, Render, RESTful API
* **Database:** MongoDB Atlas (Mongoose ORM)
* **Security:** JSON Web Tokens (JWT), Bcrypt Password Hashing

## ✨ Key Features
* **Advanced Content Creation:** Users can publish recipes with dynamic ingredient blueprints and step-by-step instructions.
* **Discovery Engine:** Real-time text filtering and tag-based search indexing.
* **Social Interaction:** Integrated 5-star rating system and comment threads on individual recipe cards.
* **Authentication:** Secure user registration, login flows, and isolated private recipe vaults.

## 💻 Local Setup Instructions
1. Unzip the project folder.
2. Open a terminal in the `recipebox-backend` folder, run `npm install`, then run `npm start` (or `node server.js`) to boot the backend.
3. Open a second terminal in the `recipebox-frontend` folder, run `npm install`, then run `npm start` to launch the React UI.
4. Open `http://localhost:3000` in your browser.
